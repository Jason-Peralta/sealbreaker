# Sealbreaker (working title)

An original, Terraria-inspired high-fantasy Minecraft Java modpack in which every gameplay mod is written by us: boss-gated progression, swings-and-hitboxes combat with believable movesets, an earned movement kit, reforging and accessory combining, dungeons and realms. Built for a private four-player server.

- Minecraft **26.2** on **NeoForge**, Java 25, ModDevGradle. See `docs/01-research.md` for why.
- Design: `docs/02-prd.md`. Plan: `docs/03-roadmap.md`. Decisions: `docs/decisions/`.
- Working conventions for humans and Claude Code sessions: `CLAUDE.md`.

## Build and run

Requires a JDK 17+ on the PATH to run Gradle (the Java 25 toolchain is downloaded automatically) and an internet connection for the first build.

```bash
./gradlew build                       # compile and package every module
./gradlew :sb_core:runClient          # launch the client with the mods loaded
./gradlew :sb_core:runServer          # dedicated server (accept the EULA in sb_core/run/eula.txt first)
./gradlew :sb_core:runGameTestServer  # headless game tests
```

IntelliJ IDEA: open the root folder as a Gradle project; run configurations are generated by ModDevGradle.

## Layout

One Gradle subproject per mod (`sb_core`, and from Milestone 1 `sb_combat`, `sb_gear`, `sb_bosses`, `sb_world`, `sb_realms`, `sb_hub`, `sb_blocks`), the packwiz modpack in `pack/`, documentation in `docs/`.

## Status

Milestone 0 (environment and spikes) in progress. `sb_core` is a hello-world module that builds and loads; see `docs/spikes/` for the five technical spikes that gate Milestone 1.

## Licence

Code: MIT. Assets (textures, models, sounds, structures, lore): all rights reserved. Private repository until decided otherwise.
